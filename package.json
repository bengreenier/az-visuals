{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "npx lerna run --stream test -- --if-present",
    "start-tm-dev": "npx lerna run --scope tm.az-visuals.com start"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
